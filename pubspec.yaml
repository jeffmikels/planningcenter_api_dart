name: planningcenter_api
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com

# remove appid and secret from test before publishing
# publish_to: none

environment:
  sdk: '>=2.14.2 <3.0.0'


dependencies:
  http:

dev_dependencies:
  lints: ^1.0.0
  test: ^1.16.0
